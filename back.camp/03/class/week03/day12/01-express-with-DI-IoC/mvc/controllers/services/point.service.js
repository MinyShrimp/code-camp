/**
 * 가진 포인트를 관리하는 Point Class
 */
export default class PointService {
    constructor() {}

    /**
     * 가진 돈을 검증
     * @returns 돈이 충분한지 여부
     */
    hasMoney = () => {
        console.log("포인트가 있는지 검증합니다.");
        return true;
    };
}
